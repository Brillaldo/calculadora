global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "fastapi-backend"
    metrics_path: /metrics
    static_configs:
      - targets: ["web-3-calculadora-1:8000"]  # alias definido en tu compose principal
